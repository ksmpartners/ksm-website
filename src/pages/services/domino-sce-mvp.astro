---
import PageLayout from '../../layouts/PageLayout.astro';
---
<PageLayout
  title="Domino SCE MVP Deployment | KSM Technology Partners"
  description="KSM delivers a validated, production-ready Domino Statistical Computing Environment in 27 weeks — integrated with your infrastructure, qualified to GxP standards, and adopted by your teams."
>

  <!-- ============================================================
       Section 1: Hero
       ============================================================ -->
  <section class="hero">
    <span class="overline">Domino SCE MVP Deployment</span>
    <h1>Choosing Domino is the first step. Now let's finish the journey.</h1>
    <p>
      Domino gives your statistical programmers a modern, validated-ready platform for GxP-regulated clinical work.
      Getting it deployed, integrated, and validated in your environment is where the real work begins —
      and where KSM comes in.
    </p>
    <div class="hero-actions">
      <a href="../contact" class="btn">Talk to a KSM expert</a>
      <a href="../services/sce-scoping" class="btn btn-outline">See how we start — our 12-week scoping engagement &rarr;</a>
    </div>
  </section>

  <!-- ============================================================
       Section 2: The Problem
       ============================================================ -->
  <div class="section-pale">
    <div class="section">
      <div class="section-heading">
        <span class="overline">The challenge</span>
        <h2>The platform decision is made. What comes next determines whether it pays off.</h2>
        <p>
          Selecting Domino is a significant and well-informed decision. But the path from contract to validated
          production environment involves infrastructure, integration, documentation, qualification, and organizational
          change — all moving in parallel. How well that journey is managed shapes how quickly your teams get productive
          and how defensible your environment is at inspection.
        </p>
      </div>
      <div class="challenge-list">

        <div class="challenge-item">
          <span class="challenge-num">01</span>
          <div class="challenge-body">
            <h3>Integration doesn't happen automatically</h3>
            <p>GitHub, NetApp ONTAP, SSO/IdP, S3 or Azure Blob Storage, firewall configuration — each connection requires configuration, testing, and in many cases, qualification evidence. Every organization's environment is different, and the details matter.</p>
          </div>
        </div>

        <div class="challenge-item">
          <span class="challenge-num">02</span>
          <div class="challenge-body">
            <h3>Validation responsibility sits with you</h3>
            <p>Domino can provide Infrastructure Qualification evidence for their managed cloud platform. Operational and Performance Qualification must be executed by your organization to satisfy GxP requirements. That work needs templates, structure, and someone who has done it before.</p>
          </div>
        </div>

        <div class="challenge-item">
          <span class="challenge-num">03</span>
          <div class="challenge-body">
            <h3>Adoption requires more than access</h3>
            <p>Statistical programmers who've worked in SAS for a decade don't change workflows because a new platform is available. Git is genuinely unfamiliar. New environments are disorienting. The organizations that get value from Domino fastest treat adoption as a people challenge from day one.</p>
          </div>
        </div>

        <div class="challenge-item">
          <span class="challenge-num">04</span>
          <div class="challenge-body">
            <h3>The scope is broader than it looks</h3>
            <p>Environment qualification, compute configuration, standard library setup, training, documentation — it adds up quickly. Building the structure to manage all of it in parallel is itself a project management discipline.</p>
          </div>
        </div>

      </div>
    </div>
  </div>

  <!-- ============================================================
       Section 3: What We Deliver
       ============================================================ -->
  <div class="section">
    <div class="section-heading">
      <span class="overline">What we deliver</span>
      <h2>A validated, production-ready SCE in 27 weeks.</h2>
      <p class="intro-para">
        KSM's Domino SCE MVP Deployment is a fixed-scope, fixed-timeline implementation built for small to midsize
        pharmaceutical companies who are ready to move from decision to production.
      </p>
    </div>

    <div class="deliverables-grid">

      <div class="deliverable-card">
        <div class="deliverable-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
        </div>
        <div class="deliverable-body">
          <h3>Validated production environment</h3>
          <p>A fully qualified Domino deployment — Development, Test, and Production environments — with IQ evidence provided by Domino and OQ/PQ seed kits your team can execute. Every environment meets GAMP 5 and 21 CFR Part 11 requirements.</p>
        </div>
      </div>

      <div class="deliverable-card">
        <div class="deliverable-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V9z"/><polyline points="14 2 14 9 20 9"/></svg>
        </div>
        <div class="deliverable-body">
          <h3>Baseline documentation package</h3>
          <p>A User Requirements Specification template, validation plan, and SCE runbook — structured and pre-populated based on our experience across multiple implementations. A proven starting point your QA team can work from, not a blank page.</p>
        </div>
      </div>

      <div class="deliverable-card">
        <div class="deliverable-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><rect x="2" y="3" width="20" height="14" rx="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
        </div>
        <div class="deliverable-body">
          <h3>Compute environments</h3>
          <p>Five baseline compute environments configured, validated, and ready for statistical programming work. Extensible as your needs evolve.</p>
        </div>
      </div>

      <div class="deliverable-card">
        <div class="deliverable-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><circle cx="18" cy="18" r="3"/><circle cx="6" cy="6" r="3"/><path d="M13 6h3a2 2 0 0 1 2 2v7"/><line x1="6" y1="9" x2="6" y2="21"/></svg>
        </div>
        <div class="deliverable-body">
          <h3>Git integration</h3>
          <p>Repository structure, branching strategy, and integration with your chosen Git platform. KSM handles both the technical configuration and the training that makes adoption stick.</p>
        </div>
      </div>

      <div class="deliverable-card deliverable-card--wide">
        <div class="deliverable-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M22 10v6M2 10l10-5 10 5-10 5z"/><path d="M6 12v5c3 3 9 3 12 0v-5"/></svg>
        </div>
        <div class="deliverable-body">
          <h3>Training</h3>
          <p>Platform training for statistical programmers and system administrators. Designed to build genuine proficiency, not just checkbox completion.</p>
        </div>
      </div>

    </div>
  </div>

  <!-- ============================================================
       Section 4: What KSM Brings
       ============================================================ -->
  <div class="section-pale">
    <div class="section">
      <div class="section-heading">
        <span class="overline">Our approach</span>
        <h2>We've walked this path before. Here's what that means for you.</h2>
        <p class="intro-para">
          Domino is the SCE engine. KSM's job is integrating it with your environment — your infrastructure, your QMS,
          your team, your regulatory context. That integration is where implementations succeed or stall.
        </p>
      </div>

      <div class="pillars-grid">

        <div class="pillar-card">
          <div class="pillar-label">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M13 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V9z"/><polyline points="13 2 13 9 20 9"/></svg>
            Integration expertise
          </div>
          <p>We've built the connections between Domino and GitHub, NetApp, identity providers, storage platforms, and external systems across multiple engagements. We know where the complexity hides and how to navigate it.</p>
        </div>

        <div class="pillar-card">
          <div class="pillar-label">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
            Validation fluency
          </div>
          <p>Our team understands the distinction between IQ, OQ, and PQ — and why it matters for your regulatory obligations. We provide the templates and structure. Your quality team executes and owns the evidence. That's the only arrangement that works for an FDA inspection.</p>
        </div>

        <div class="pillar-card">
          <div class="pillar-label">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
            Change management
          </div>
          <p>Every implementation we run treats programmer adoption as a first-class problem. We use change champions, involve programmers in decisions that affect their daily work, and invest in training that builds real capability. Platforms don't fail — adoption does.</p>
        </div>

        <div class="pillar-card">
          <div class="pillar-label">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
            Incremental delivery
          </div>
          <p>We don't wait until week 27 to show you something working. Delivery is structured so your team has something usable early — a pilot environment, a working workflow, a first study — and each milestone builds on the last.</p>
        </div>

      </div>

      <p class="credibility-line">
        KSM experts are active contributors to the Domino SCE Coalition and have led validated SCE implementations
        at pharmaceutical companies ranging from emerging biotech to global enterprise.
      </p>
    </div>
  </div>

  <!-- ============================================================
       Section 5: Lessons Learned
       ============================================================ -->
  <div class="section">
    <div class="section-heading">
      <span class="overline">Lessons learned</span>
      <h2>What we've learned from doing this, not just advising on it.</h2>
    </div>

    <div class="lessons-grid">

      <div class="lesson-item">
        <div class="lesson-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><polyline points="9 11 12 14 22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>
        </div>
        <div>
          <h3>Involve QA before you think you need to</h3>
          <p>QA teams brought in after design decisions are made create friction and rework. The implementations that go smoothly have Quality at the table in week one.</p>
        </div>
      </div>

      <div class="lesson-item">
        <div class="lesson-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
        </div>
        <div>
          <h3>IT and business alignment isn't optional</h3>
          <p>A technically sound deployment that IT doesn't trust, or that business users won't adopt, is a deployment that hasn't delivered value. Both sides need to be invested from the start.</p>
        </div>
      </div>

      <div class="lesson-item">
        <div class="lesson-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><circle cx="18" cy="18" r="3"/><circle cx="6" cy="6" r="3"/><path d="M13 6h3a2 2 0 0 1 2 2v7"/><line x1="6" y1="9" x2="6" y2="21"/></svg>
        </div>
        <div>
          <h3>Git is harder than it looks</h3>
          <p>For programmers without software development backgrounds, Git concepts are genuinely unintuitive. Plan for meaningful training investment. Don't assume exposure equals proficiency.</p>
        </div>
      </div>

      <div class="lesson-item">
        <div class="lesson-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
        </div>
        <div>
          <h3>Start narrow, expand deliberately</h3>
          <p>Organizations that try to cover every use case in the first deployment get stuck. Start with one pilot study. Prove the workflow. Then expand. Incremental delivery isn't a compromise — it's how durable implementations are built.</p>
        </div>
      </div>

      <div class="lesson-item lesson-item--wide">
        <div class="lesson-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><rect x="2" y="3" width="20" height="14" rx="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
        </div>
        <div>
          <h3>Domino is a platform, not an application</h3>
          <p>This is the mental model shift that matters most. You're not deploying a tool — you're building an operating environment. The configuration decisions you make in implementation shape every workflow that runs on top of it for years.</p>
        </div>
      </div>

    </div>
  </div>

  <!-- ============================================================
       Section 6: Is This the Right Engagement?
       ============================================================ -->
  <div class="section-pale">
    <div class="section">
      <div class="fit-layout">
        <div>
          <span class="overline">Is this right for you?</span>
          <h2>This offering is designed for a specific moment in your journey.</h2>
          <p>
            Our Domino SCE MVP Deployment is built for organizations that have made the decision to adopt Domino
            and are ready to move into implementation. It's a structured, scoped path from committed decision to
            validated production.
          </p>
          <p>
            If you're earlier in the process — still scoping requirements, evaluating architecture options, or trying
            to understand what a full implementation would actually involve — our 12-week SCE Scoping Engagement is
            the right first step. It's how most of our Domino deployments begin.
          </p>
          <a href="../services/sce-scoping" class="btn btn-ghost mt-2">Explore the 12-week SCE Scoping Engagement &rarr;</a>
        </div>
        <div class="readiness-card">
          <div class="readiness-heading">Ready for this engagement if:</div>
          <ul class="readiness-list">
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><polyline points="20 6 9 17 4 12"/></svg>
              You have a signed Domino contract or are in final negotiations
            </li>
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><polyline points="20 6 9 17 4 12"/></svg>
              Your requirements are understood and documented
            </li>
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><polyline points="20 6 9 17 4 12"/></svg>
              IT, QA, and business stakeholders are aligned on goals
            </li>
            <li>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><polyline points="20 6 9 17 4 12"/></svg>
              Infrastructure access and environment provisioning can begin within weeks
            </li>
          </ul>
          <div class="readiness-divider"></div>
          <div class="readiness-heading readiness-heading--alt">Not ready yet? Start here:</div>
          <a href="../services/sce-scoping" class="readiness-link">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
            12-Week SCE Scoping Engagement
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- ============================================================
       Section 7: CTA Block
       ============================================================ -->
  <div class="cta-banner">
    <h2>You've made the right platform decision. Let's see it through.</h2>
    <p>
      Let's talk through where your organization is today and what a realistic path to production looks like.
    </p>
    <div class="hero-actions">
      <a href="../contact" class="btn btn-outline">Talk to a KSM expert &rarr;</a>
      <a href="../services/sce-scoping" class="cta-secondary-link">Start with our 12-week Scoping Engagement &rarr;</a>
    </div>
  </div>

</PageLayout>

<style>
  /* Section heading with lead paragraph */
  .section-heading p {
    font-size: var(--text-lg);
    color: var(--color-slate-gray);
    max-width: 720px;
    margin-bottom: 0;
    line-height: 1.7;
  }

  /* Intro paragraphs */
  .intro-para {
    font-size: 1.05rem;
    color: var(--color-text-muted);
    max-width: 720px;
    margin-bottom: 2rem;
  }

  /* ---- Challenge list ---- */
  .challenge-list {
    display: block;
    margin-top: var(--space-8);
    border-top: 1px solid rgba(178, 209, 234, 0.5);
  }

  .challenge-item {
    display: flex;
    gap: var(--space-6);
    padding: var(--space-6) 0;
    border-bottom: 1px solid rgba(178, 209, 234, 0.5);
    align-items: flex-start;
  }

  .challenge-num {
    font-family: var(--font-mono);
    font-size: var(--text-2xl);
    font-weight: 700;
    color: var(--color-blue-pale);
    line-height: 1;
    flex-shrink: 0;
    width: 2.5rem;
    padding-top: 0.1em;
  }

  .challenge-body h3 {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--color-slate);
    letter-spacing: -0.02em;
    margin-bottom: var(--space-2);
  }

  .challenge-body p {
    color: var(--color-slate-gray);
    font-size: var(--text-base);
    line-height: 1.7;
    margin-bottom: 0;
    max-width: 72ch;
  }

  @media (min-width: 768px) {
    .challenge-item {
      gap: var(--space-8);
    }
  }

  /* ---- What We Deliver ---- */
  .deliverables-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-4);
    margin-top: var(--space-6);
  }

  @media (min-width: 768px) {
    .deliverables-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .deliverable-card {
    display: flex;
    gap: var(--space-4);
    background: var(--color-surface);
    border: 1px solid rgba(178, 209, 234, 0.4);
    border-radius: var(--radius-md);
    padding: var(--space-6) var(--space-8);
    box-shadow: var(--shadow-sm);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .deliverable-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  @media (min-width: 768px) {
    .deliverable-card--wide {
      grid-column: 1 / -1;
      max-width: calc(50% - var(--space-2));
    }
  }

  .deliverable-icon {
    width: 40px;
    height: 40px;
    background: var(--color-teal-light);
    border-radius: var(--radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-teal);
    flex-shrink: 0;
  }

  .deliverable-body h3 {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--color-slate);
    letter-spacing: -0.02em;
    margin-bottom: var(--space-2);
  }

  .deliverable-body p {
    color: var(--color-slate-gray);
    font-size: var(--text-base);
    line-height: 1.65;
    margin-bottom: 0;
  }

  /* ---- Pillars (What KSM Brings) ---- */
  .pillars-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-6);
    margin-bottom: var(--space-10);
  }

  @media (min-width: 768px) {
    .pillars-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .pillars-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .pillar-card {
    background: var(--color-surface);
    border-radius: var(--radius-md);
    border: 1px solid rgba(178, 209, 234, 0.4);
    box-shadow: var(--shadow-md);
    padding: var(--space-8);
    position: relative;
    overflow: hidden;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .pillar-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }

  .pillar-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: var(--space-8);
    width: 32px;
    height: 3px;
    border-radius: 0 0 var(--radius-sm) var(--radius-sm);
    background: var(--color-blue);
  }

  .pillar-card:nth-child(2)::before { background: var(--color-teal); }
  .pillar-card:nth-child(3)::before { background: var(--color-blue-mid); }
  .pillar-card:nth-child(4)::before { background: var(--color-blue-pale); }

  .pillar-label {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-slate);
    letter-spacing: -0.01em;
    margin-bottom: var(--space-4);
  }

  .pillar-label svg {
    color: var(--color-teal);
    flex-shrink: 0;
  }

  .pillar-card p {
    color: var(--color-slate-gray);
    font-size: var(--text-base);
    line-height: 1.65;
    margin-bottom: 0;
  }

  .credibility-line {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--color-slate-gray);
    border-left: 3px solid var(--color-blue-pale);
    padding-left: var(--space-4);
    margin-bottom: 0;
  }

  /* ---- Lessons Learned ---- */
  .lessons-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-6);
    margin-top: var(--space-8);
  }

  @media (min-width: 768px) {
    .lessons-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .lesson-item {
    display: flex;
    gap: var(--space-4);
    align-items: flex-start;
    padding: var(--space-6) var(--space-8);
    background: var(--color-surface);
    border: 1px solid rgba(178, 209, 234, 0.4);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-sm);
  }

  @media (min-width: 768px) {
    .lesson-item--wide {
      grid-column: 1 / -1;
      max-width: calc(50% - var(--space-3));
    }
  }

  .lesson-icon {
    width: 36px;
    height: 36px;
    background: var(--color-teal-light);
    border-radius: var(--radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-teal);
    flex-shrink: 0;
  }

  .lesson-item h3 {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--color-slate);
    letter-spacing: -0.02em;
    margin-bottom: var(--space-2);
  }

  .lesson-item p {
    color: var(--color-slate-gray);
    font-size: var(--text-base);
    line-height: 1.65;
    margin-bottom: 0;
  }

  /* ---- Is This Right For You ---- */
  .fit-layout {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-12);
    align-items: start;
  }

  @media (min-width: 768px) {
    .fit-layout {
      grid-template-columns: 1fr 1fr;
    }
  }

  .fit-layout p {
    color: var(--color-slate-gray);
    font-size: var(--text-base);
    line-height: 1.7;
  }

  .readiness-card {
    background: var(--color-surface);
    border: 1px solid rgba(178, 209, 234, 0.4);
    border-radius: var(--radius-md);
    padding: var(--space-8);
    box-shadow: var(--shadow-sm);
  }

  .readiness-heading {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-teal);
    margin-bottom: var(--space-4);
  }

  .readiness-heading--alt {
    color: var(--color-slate-gray);
  }

  .readiness-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .readiness-list li {
    display: flex;
    gap: var(--space-3);
    align-items: flex-start;
    color: var(--color-slate-gray);
    font-size: var(--text-base);
    line-height: 1.55;
  }

  .readiness-list svg {
    color: var(--color-teal);
    flex-shrink: 0;
    margin-top: 0.2em;
  }

  .readiness-divider {
    height: 1px;
    background: var(--color-blue-pale);
    margin: var(--space-6) 0;
    opacity: 0.5;
  }

  .readiness-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    color: var(--color-blue);
    font-size: var(--text-sm);
    font-weight: 600;
    text-decoration: none;
    transition: color 0.15s;
  }

  .readiness-link:hover {
    color: var(--color-teal);
    text-decoration: none;
  }

  .readiness-link svg {
    transition: transform 0.15s;
  }

  .readiness-link:hover svg {
    transform: translateX(3px);
  }

  /* CTA secondary link */
  .cta-secondary-link {
    color: var(--color-blue-pale);
    font-size: var(--text-sm);
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    transition: color 0.15s;
  }

  .cta-secondary-link:hover {
    color: white;
    text-decoration: none;
  }

  .mt-2 {
    display: inline-flex;
    margin-top: var(--space-6);
  }
</style>
